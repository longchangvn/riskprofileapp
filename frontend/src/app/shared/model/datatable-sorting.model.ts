export class DataTableSorting {
  // This class is used for sorts attribute on ngx-datatable '[sorts]="[{prop: 'ID', dir: 'asc'}]"'
  // for those whose use sort attribute can use the class.
  // prop: sort by column name
  // dir: sort order ASC or DESC
  prop: string;
  dir: string;
}
