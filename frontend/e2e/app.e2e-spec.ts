import { StepOnePage } from './app.po';

describe('StepOne App', () => {
  let page: StepOnePage;

  beforeEach(() => {
    page = new StepOnePage();
  });

  it('should expect true to be true', () => {
    expect(true).toBe(true);
  });
});
